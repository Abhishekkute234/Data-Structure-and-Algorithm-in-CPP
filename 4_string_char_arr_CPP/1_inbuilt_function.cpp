//   different inbuilt operations in the strinng

/*
length()-->	This function returns the length of 
             the string.

swap() -->	This function is used to swap the
              values of 2 strings.

size() -->	Used to find the size of string

resize()-->	This function is used to resize
             the length of the string up to the
             given number of characters.

find()	--->Used to find the string which is 
              passed in parameters

push_back()--->	This function is used to 
               push the passed character at
              the end of the string
             
pop_back()--->This function is used to pop
              the last character from the
                  string

clear() --->	This function is used to remove all 
             the elements of the string.

*/
